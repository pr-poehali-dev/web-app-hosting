CREATE TABLE IF NOT EXISTS t_p91355423_web_app_hosting.messages (id BIGSERIAL PRIMARY KEY, channel VARCHAR(50) NOT NULL, user_id INTEGER NOT NULL REFERENCES t_p91355423_web_app_hosting.users(id), text TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), hidden BOOLEAN NOT NULL DEFAULT FALSE);