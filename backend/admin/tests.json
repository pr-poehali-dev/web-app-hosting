{
  "tests": [
    {
      "name": "GET /stats без токена — 403",
      "method": "GET",
      "path": "/?action=stats",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ запрещён"},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET /users без токена — 403",
      "method": "GET",
      "path": "/?action=users",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ запрещён"},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET /chat_settings без токена — 403",
      "method": "GET",
      "path": "/?action=chat_settings",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ запрещён"},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET /user_receipts без токена — 403",
      "method": "GET",
      "path": "/?action=user_receipts&user_id=1",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ запрещён"},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET /gdpr_export без токена — 403",
      "method": "GET",
      "path": "/?action=gdpr_export&user_id=1",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ запрещён"},
      "bodyMatcher": "partial"
    }
  ]
}
